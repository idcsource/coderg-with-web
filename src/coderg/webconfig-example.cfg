[server]
	port = 12121							# 必需
	https = false							# 如果为true，sslcert和sslkey为必需
	sslcert = cert.pem
	sslkey = key.pem

[database]
	server = 127.0.0.1						# 必需
	port = 5432								# 必需
	user = coderg							# 必需
	passwd = coderg							# 必需
	dbname = coderg-web						# 必需

[web]
	name = CoderG
	titleKey = Go语言 HTTP Web B/S 框架
	copyright = 忆黛蒙逝·建造源 2015年 Powered By CoderG

[admin]
	cookie = coderg
	nologin = /Admin/login  # 如果没有登录则返回这个页面
